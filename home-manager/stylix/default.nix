# {
#   username,
#   ...
# }:
{
  stylix.targets = {
    nixcord.enable = false;
    vesktop.enable = false;
    vscode.enable = false;
    waybar.enable = false;
    # floorp = {
    #   enable = true;
    #   profileNames = [ "${username}" ];
    # };
  };
}
